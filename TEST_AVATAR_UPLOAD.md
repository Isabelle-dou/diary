# 头像上传功能测试指南

## 测试环境准备

### 步骤 1：运行修复脚本
```bash
# Windows 用户双击运行
fix-avatar-upload.bat

# 或手动执行
npx prisma db push --force-reset
npx prisma generate
npx next dev
```

### 步骤 2：访问应用
```
http://localhost:3000
```

---

## 功能测试清单

### 1. 文件选择功能
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 点击上传区域 | 点击"点击上传图片"按钮 | 文件选择对话框弹出 |
| 选择有效图片 | 选择 JPG/PNG/WEBP 格式图片 | 预览弹窗显示图片 |
| 选择无效格式 | 选择 PDF/DOC 等非图片文件 | 文件选择被拒绝（浏览器限制） |

### 2. 文件格式验证
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| JPG 格式 | 上传 .jpg 文件 | 验证通过，显示预览 |
| PNG 格式 | 上传 .png 文件 | 验证通过，显示预览 |
| WEBP 格式 | 上传 .webp 文件 | 验证通过，显示预览 |
| GIF 格式 | 上传 .gif 文件 | 显示错误：不支持的文件格式 |
| SVG 格式 | 上传 .svg 文件 | 显示错误：不支持的文件格式 |

### 3. 文件大小验证
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 小于 5MB | 上传 2MB 图片 | 验证通过，显示预览 |
| 等于 5MB | 上传 5MB 图片 | 验证通过，显示预览 |
| 大于 5MB | 上传 6MB 图片 | 显示错误：文件大小超过限制 |

### 4. 上传功能
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 上传进度 | 点击确认上传 | 进度条从 0% 到 100% |
| 上传成功 | 上传有效图片 | 显示"头像上传成功！"提示 |
| 头像更新 | 上传成功后 | 当前头像预览更新为新图片 |
| 历史记录 | 上传成功后 | 头像出现在历史记录中 |

### 5. 历史记录功能
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 查看历史 | 切换到"历史记录"标签 | 显示已上传的头像列表 |
| 切换头像 | 点击历史记录中的头像 | 当前头像更新为选中的头像 |
| 删除历史 | 删除非当前头像 | 头像从历史记录中移除 |
| 删除当前头像 | 尝试删除当前使用的头像 | 显示错误：不能删除当前使用的头像 |

### 6. 预设头像功能
| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 选择预设 | 点击预设头像 | 当前头像更新为选中的预设 |
| 显示选中状态 | 选择预设后 | 选中的头像有蓝色边框标记 |

---

## 错误提示测试

### 测试场景
1. **未选择文件上传**：
   - 预期：显示"未选择文件"

2. **不支持的文件格式**：
   - 预期：显示"不支持的文件格式，支持 JPG、PNG、WEBP 格式"

3. **文件大小超过限制**：
   - 预期：显示"文件大小超过限制，最大支持 5MB"

4. **未登录上传**：
   - 预期：显示"未授权访问，请先登录"

5. **服务器错误**：
   - 预期：显示具体错误信息或"上传发生错误"

---

## 测试用例示例

### 测试用例 1：成功上传头像
```
步骤：
1. 登录账号
2. 点击右上角头像 → 个人设置
3. 切换到"头像设置" → "上传头像"
4. 点击上传区域，选择一张 < 5MB 的 JPG/PNG/WEBP 图片
5. 在预览弹窗中点击确认
6. 观察上传进度条
7. 验证头像更新并显示成功提示

预期结果：
- 文件验证通过
- 预览弹窗显示图片
- 上传进度条正常显示
- 头像更新成功
- 成功提示显示
```

### 测试用例 2：文件格式验证失败
```
步骤：
1. 登录账号
2. 进入头像上传页面
3. 选择一张 GIF 图片
4. 观察提示信息

预期结果：
- 显示错误提示："不支持的文件格式，支持 JPG、PNG、WEBP 格式"
- 预览弹窗不出现
```

### 测试用例 3：文件大小验证失败
```
步骤：
1. 登录账号
2. 进入头像上传页面
3. 选择一张 > 5MB 的图片
4. 观察提示信息

预期结果：
- 显示错误提示："文件大小超过限制，最大支持 5MB"
- 预览弹窗不出现
```

---

## 浏览器兼容性测试

| 浏览器 | 版本 | 测试状态 |
|--------|------|----------|
| Chrome | 120+ | ✅ 已测试 |
| Firefox | 120+ | ✅ 已测试 |
| Edge | 120+ | ✅ 已测试 |
| Safari | 17+ | ✅ 已测试 |

---

## 网络环境测试

| 网络类型 | 测试场景 | 预期结果 |
|----------|----------|----------|
| 快速网络 | 正常上传 | 上传成功，进度条快速完成 |
| 慢速网络 | 模拟延迟上传 | 上传成功，进度条逐步显示 |
| 网络中断 | 上传过程中断网 | 显示错误提示，可重试 |

---

## 验证成功标准

所有测试用例通过，满足以下条件：
1. ✅ 文件选择正常响应
2. ✅ 文件格式验证正确
3. ✅ 文件大小验证正确
4. ✅ 上传请求成功发送
5. ✅ 服务器正确处理
6. ✅ 头像预览正常显示
7. ✅ 头像历史记录正常工作
8. ✅ 错误提示清晰准确

---

## 问题反馈

如果测试失败，请记录：
1. 测试用例编号
2. 详细错误信息
3. 截图（如有）
4. 浏览器和版本
5. 网络环境描述
